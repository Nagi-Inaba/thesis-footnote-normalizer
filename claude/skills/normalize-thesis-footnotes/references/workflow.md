# Workflow gates

1. Treat first/repeat results as provisional until body order is stable.
2. Require an approved policy before proposing a normalized form.
3. Require a stable `source_id` before treating different strings as the same work.
4. Keep ambiguous identity and specialist forms for human approval.
5. Rerun after Word edits and after every structural change.
