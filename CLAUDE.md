# Claude Code entrypoint

Use the skill at `claude/skills/normalize-thesis-footnotes/SKILL.md` for thesis-footnote audits.

Follow `AGENTS.md` for the shared safety contract.

The PowerShell audit is the evidence source. Do not replace its counts or classifications with an unsupported language-model judgment.
